@Library('my-shared-library') _

pipeline {
    agent any
    tools {
        maven 'maven-3'
        dockerTool 'Docker'
    }
    environment {
        // تحديث اسم المستخدم هنا ليتطابق مع الحساب
        DOCKER_HUB_CREDS = "docker-hub-credentials-id"
        IMAGE_NAME = "nourhanosama/my-app:${env.BUILD_ID}" 
        DEPLOYMENT_FILE = "CI-CD/lab23/deployment.yaml"
    }

    stages {
        stage('Checkout 🛰️') {
            steps {
                checkout([$class: 'GitSCM', 
                    branches: scm.branches,
                    userRemoteConfigs: scm.userRemoteConfigs,
                    extensions: [[$class: 'CheckoutOption', timeout: 20], 
                                 [$class: 'CloneOption', timeout: 20]]
                ])
            }
        }

        stage('Project Operations') {
            steps {
                dir('CI-CD/lab23') {
                    script {
                        stage('Unit Test 🧪') {
                            runUnitTest()
                        }
                        stage('Build Artifact 🏗️') {
                            buildApp()
                        }
                        stage('Docker Build 🐳') {
                            withCredentials([usernamePassword(credentialsId: DOCKER_HUB_CREDS, passwordVariable: 'DOCKER_PASS', usernameVariable: 'DOCKER_USER')]) {
                                sh "echo \$DOCKER_PASS | docker login -u \$DOCKER_USER --password-stdin"
                                buildImage(IMAGE_NAME)
                            }
                        }
                        stage('Security Scan 🔍') {
                            // سنضع محاولة الفحص داخل try-catch حتى لا يتوقف الـ Pipeline إذا لم يكن Trivy مثبتاً
                            try {
                                scanImage(IMAGE_NAME)
                            } catch (Exception e) {
                                echo "تنبيه: أداة Trivy غير موجودة، سيتم تخطي الفحص حالياً."
                            }
                        }
                        stage('Push to DockerHub 🚀') {
                            pushImage(IMAGE_NAME, DOCKER_HUB_CREDS)
                        }
                    }
                }
            }
        }

        stage('Cleanup 🧹') {
            steps {
                removeImage(IMAGE_NAME)
            }
        }

        stage('Deploy to K8s ⛵') {
            steps {
                deployOnK8s(IMAGE_NAME, DEPLOYMENT_FILE)
            }
        }
    }
}
