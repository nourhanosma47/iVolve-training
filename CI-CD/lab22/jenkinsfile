pipeline {
    agent any

    tools {
        maven 'maven-3'
        dockerTool 'Docker' // ⚠️ تأكدي أن الاسم مطابق لما كتبتيه في Tools
    }

    environment {
        DOCKER_HUB_USER = 'your-docker-username' 
        IMAGE_NAME      = 'jenkins-app'
        IMAGE_TAG       = "${env.BUILD_NUMBER}"
        DOCKER_CREDS_ID = 'docker-hub-creds'
    }

    stages {
        stage('1. Clone Source Code') {
            steps {
                echo 'جاري سحب الكود... 📥'
                git branch: 'main', url: 'https://github.com/Ibrahim-Adel15/Jenkins_App.git'
            }
        }

        stage('2. Run Unit Test') {
            steps {
                echo 'جاري تشغيل الاختبارات... 🧪'
                sh 'mvn test'
            }
        }

        stage('3. Build App') {
            steps {
                echo 'جاري بناء التطبيق... 🏗️'
                sh 'mvn clean package -DskipTests'
            }
        }

        stage('4. Build Docker image') {
            steps {
                script {
                    // تعريف مسار Docker لضمان عمل الأمر داخل الحاوية
                    def dockerHome = tool name: 'Docker', type: 'dockerTool'
                    withEnv(["PATH+DOCKER=${dockerHome}/bin"]) {
                        echo 'جاري بناء صورة Docker... 🐳'
                        sh "docker build -t ${DOCKER_HUB_USER}/${IMAGE_NAME}:${IMAGE_TAG} ."
                    }
                }
            }
        }

        stage('5. Push image to Docker hub') {
            steps {
                script {
                    def dockerHome = tool name: 'Docker', type: 'dockerTool'
                    withEnv(["PATH+DOCKER=${dockerHome}/bin"]) {
                        echo 'جاري رفع الصورة إلى Docker Hub... 🚀'
                        withCredentials([usernamePassword(credentialsId: "${DOCKER_CREDS_ID}", passwordVariable: 'PASS', usernameVariable: 'USER')]) {
                            sh "echo ${PASS} | docker login -u ${USER} --password-stdin"
                            sh "docker push ${DOCKER_HUB_USER}/${IMAGE_NAME}:${IMAGE_TAG}"
                        }
                    }
                }
            }
        }

        stage('6. Delete image locally') {
            steps {
                script {
                    def dockerHome = tool name: 'Docker', type: 'dockerTool'
                    withEnv(["PATH+DOCKER=${dockerHome}/bin"]) {
                        echo 'جاري تنظيف الصور المحلية... 🧹'
                        sh "docker rmi ${DOCKER_HUB_USER}/${IMAGE_NAME}:${IMAGE_TAG}"
                    }
                }
            }
        }

        stage('7. Edit deployment.yaml') {
            steps {
                echo 'تحديث ملف الـ Deployment... 📝'
                sh "sed -i 's|image: .*|image: ${DOCKER_HUB_USER}/${IMAGE_NAME}:${IMAGE_TAG}|g' deployment.yaml"
            }
        }

        stage('8. Deploy to k8s cluster') {
            steps {
                echo 'جاري النشر على Kubernetes... ⛵'
                sh "kubectl apply -f deployment.yaml"
            }
        }
    }

    post {
        success { echo 'تم تنفيذ المهمة بنجاح! 🎉' }
        failure { echo 'فشل الـ Pipeline، يرجى مراجعة الـ Logs. ❌' }
    }
}
