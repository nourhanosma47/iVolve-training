@Library('my-shared-library') _

pipeline {
    agent { label 'slave-vm' } 

    environment {
        DOCKER_HUB_CREDS = "docker-hub-credentials-id" 
        K8S_CREDS_ID     = "kubeconfig-id" 
        
        // تمييز الصورة والـ Namespace بناءً على الفرع الحالي
        IMAGE_NAME       = "nourhanosama/jenkins-app:${env.BRANCH_NAME}-${env.BUILD_ID}" 
        K8S_NAMESPACE    = "${env.BRANCH_NAME ?: 'main'}"
    }

    stages {
        stage('Checkout 🛰️') {
            steps {
                checkout scm 
            }
        }

        stage('CI: Build & Test 🧪') {
            steps {
                script {
                    dir('CI-CD/lab24') { 
                        echo "جاري تشغيل الاختبارات وبناء التطبيق... 🧪"
                        runUnitTest()
                        buildApp()
                        
                        withCredentials([usernamePassword(credentialsId: DOCKER_HUB_CREDS, passwordVariable: 'DOCKER_PASS', usernameVariable: 'DOCKER_USER')]) {
                            sh "echo \$DOCKER_PASS | docker login -u \$DOCKER_USER --password-stdin"
                            
                            echo "جاري بناء صورة Docker... 🐳"
                            buildImage(IMAGE_NAME)
                            scanImage(IMAGE_NAME)
                        }
                    }
                }
            }
        }

        stage('Push to Registry 🚀') {
            steps {
                script {
                    echo "جاري رفع الصورة إلى Docker Hub... 🚀"
                    pushImage(IMAGE_NAME, DOCKER_HUB_CREDS)
                }
            }
        }

        stage('CD: Deploy to K8s ⛵') {
            steps {
                script {
                    dir('CI-CD/lab24') {
                        withKubeConfig([credentialsId: "${env.K8S_CREDS_ID}"]) { 
                            echo "Ensuring namespace '${K8S_NAMESPACE}' exists..."
                            sh "kubectl create namespace ${K8S_NAMESPACE} --dry-run=client -o yaml | kubectl apply -f - --insecure-skip-tls-verify"
                            
                            echo "Deploying to Namespace: ${K8S_NAMESPACE} 🏗️"
                            sh "sed -i 's|image:.*|image: ${IMAGE_NAME}|g' deployment.yaml"
                            
                            sh "kubectl apply -f deployment.yaml -n ${K8S_NAMESPACE} --insecure-skip-tls-verify"
                            sh "kubectl apply -f service.yaml -n ${K8S_NAMESPACE} --insecure-skip-tls-verify"
                            
                            echo "Checking deployment status..."
                            sh "kubectl rollout status deployment/jenkins-app -n ${K8S_NAMESPACE} --insecure-skip-tls-verify"
                        }
                    }
                }
            }
        }

        stage('Cleanup 🧹') {
            steps {
                echo "تنظيف الصور المحلية... 🧹"
                removeImage(IMAGE_NAME)
            }
        }
    }
}